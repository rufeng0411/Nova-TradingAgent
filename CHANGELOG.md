# Changelog

## [v0.2.5] - 2026-09-03

### Added

- Public AGPL tree for Nova-TradingAgent: 15-agent A-share research desk, Web workbench, optional Tushare L2, optional Qlib bridge.
- Install path: SQLite + `uv sync` + `frontend` build + uvicorn `:8000` + `GET /healthz`.
- Docs: install, user guide, configure, capabilities (no architecture book).

### 中文

- Nova-TradingAgent 以 AGPL-3.0 发布完整产品树。
- 黄金安装：SQLite + 构建前端 + 8000 端口；健康检查 `/healthz`。
