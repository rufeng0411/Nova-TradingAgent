# Roadmap

## Now

- Keep the SQLite golden path and `/healthz` identical to the install guide.
- Optional Tushare L2 remains opt-in.

## Next

- GHCR images after the `v*` docker-publish workflow has run.
- Qlib remains a local sidecar, not a Docker default.

## Later

- Star-history for **this** repository once stars exist (not upstream charts).

# 路线图

当前：安装黄金路径与健康检查保持可复制。下一步：标签构建 GHCR。Qlib 不进默认镜像。
