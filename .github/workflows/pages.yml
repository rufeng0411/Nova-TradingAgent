# GitHub Pages stays off while the repository is private.
# Re-enable deploy-pages after the factory 「全网发布」 step.
name: pages
on:
  workflow_dispatch:
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pages is disabled until the repository is public."
